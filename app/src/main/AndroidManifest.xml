<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Android UsageStatsManager -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Required for goal notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required for the background break timer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="false"
        android:label="ScrollCheck"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScrollCheck">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service
            android:name=".BreakTimerService"
            android:exported="false" />

    </application>

</manifest>
